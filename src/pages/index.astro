---
import Article from "$layouts/Article.astro";
import type { ArticleMarkdown } from "$scripts/resources.astro";

const markdown = (
    await Astro.glob("../markdown/index.md")
)[0] as ArticleMarkdown;
---

<Article markdown={markdown} />
