---
import Article from "$layouts/Article.astro";
import type { InstructionFrontmatter } from "$scripts/types";
import type { MarkdownInstance } from "astro";

const markdown = (
    await Astro.glob("../markdown/instruction.md")
)[0] as MarkdownInstance<InstructionFrontmatter>;
---

<Article markdown={markdown} />
